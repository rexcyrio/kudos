import React from "react";
import { Text } from "react-native";

function NotificationsPage(): JSX.Element {
  return <Text>This is the notifications page</Text>;
}

export default React.memo(NotificationsPage);
